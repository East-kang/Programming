# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean 54min
import sys
input = sys.stdin.readline
x, y = map(int, input().split())

if x*y==0:
	print("0")
else:
	if x>0:
		print("1" if y>0 else "4")
	else:
		print("2" if y>0 else "3")